# Atomic Test Mapping: T1059.001 — PowerShell

Validates detection rule `100010` in `rules/local_rules.xml`.

## Test

Atomic Red Team: **T1059.001-1** — PowerShell Base64 Encoded Command

```powershell
Invoke-AtomicTest T1059.001 -TestNumbers 1
```

This runs an encoded PowerShell command that decodes to a benign
`Write-Host` call, simulating the encoding technique used by malicious
scripts without executing anything harmful.

## Expected Result

- Wazuh rule `100010` fires at level 10
- Alert includes the full (encoded) command line and hostname
- Alert is enriched with MITRE ATT&CK technique `T1059.001`

## Validation Log

| Date | Tester | Result | Notes |
|------|--------|--------|-------|
| _fill in on test run_ | | | |

## Cleanup

```powershell
Invoke-AtomicTest T1059.001 -TestNumbers 1 -Cleanup
```
